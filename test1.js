const name = [{
        'name': 'Husnain',
        'age': 20,
    }].map((item) => {
        return (
            `<div>
                <h1>${item.name}</h1>
                <h1>${item.age}</h1>
            </div>`
        );
    });